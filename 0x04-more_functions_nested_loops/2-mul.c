#include "main.h"

/**
 * mul - Multiplties two Integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The resuult of multiplication between x parm and y param
*/

int mul(int x, int y)
{
	return (x*y);
}
